{
  "name": "think41-app",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack starter for Think41 interview (React + Vite + Express).",
  "scripts": {
    "install:all": "npm --prefix client install && npm --prefix server install",
    "client": "npm --prefix client run dev",
    "server": "npm --prefix server run dev",
    "dev": "concurrently \"npm:server\" \"npm:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
